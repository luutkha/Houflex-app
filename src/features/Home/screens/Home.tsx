import { Center, Text } from 'native-base'
import React from 'react'

interface Props {
    
}

export const Home = (props: Props) => {
    return (
        <Center>
            <Text>Home page</Text>
        </Center>
    )
}

