import { Center, Text } from 'native-base'
import React from 'react'

interface Props {
    
}

export const NotificationHome = (props: Props) => {
    return (
        <Center>
            <Text>Notification Home</Text>
        </Center>
    )
}
