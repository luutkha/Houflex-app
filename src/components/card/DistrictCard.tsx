import { Center } from 'native-base'
import React from 'react'

interface Props {
    
}

export const DistrictCard = (props: Props) => {
    return (
        <Center>
            
        </Center>
    )
}
